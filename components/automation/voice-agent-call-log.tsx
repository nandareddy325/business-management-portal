'use client'
// Voice agent call log — shows Vapi call history per lead
// TODO: wire to services/voice-agent.service.ts getCallLogs()

export function VoiceAgentCallLog() {
  return <div>Voice Agent Call Log — Not implemented yet</div>
}
