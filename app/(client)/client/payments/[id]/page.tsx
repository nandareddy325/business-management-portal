// Payment detail + receipt download
// TODO: fetch payment record, generate/download receipt PDF

export default function ClientPaymentDetailPage({ params }: { params: { id: string } }) {
  return <div>Payment Detail — Not implemented yet — ID: {params.id}</div>
}
