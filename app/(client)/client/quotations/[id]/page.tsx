// View + e-sign approve/reject a specific quotation
// TODO: use components/client-portal/esignature-modal.tsx

export default function ClientQuotationDetailPage({ params }: { params: { id: string } }) {
  return <div>Quotation Detail — Not implemented yet — ID: {params.id}</div>
}
